// Fog Market — mist trading — auto-forge html scaffold
// Spec: core interaction | polished UI | share/export
// Constraints: uses only one interaction type (click OR drag OR type) | must react to scroll
const stage = document.getElementById("stage");
stage.textContent = "Built by auto-forge — replace with your idea.";
console.log("auto-forge html scaffold ready", "fog-market-mist-trading");
