// Lantern Ledger — night journal — auto-forge html scaffold
// Spec: core interaction | polished UI | share/export
// Constraints: 3 colors max (plus black/white) | no external fonts — system fonts only
const stage = document.getElementById("stage");
stage.textContent = "Built by auto-forge — replace with your idea.";
console.log("auto-forge html scaffold ready", "lantern-ledger-night-journal");
