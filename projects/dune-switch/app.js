// Dune Switch — auto-forge html scaffold
// Spec: relay grid | storm timer | caravan score
// Constraints: sound on interaction (WebAudio, no assets)
const stage = document.getElementById("stage");
stage.textContent = "Built by auto-forge — replace with your idea.";
console.log("auto-forge html scaffold ready", "dune-switch");
