// Copper Kite — auto-forge html scaffold
// Spec: wind sim | kite canvas | gust code
// Constraints: sound on interaction (WebAudio, no assets) | must react to scroll
const stage = document.getElementById("stage");
stage.textContent = "Built by auto-forge — replace with your idea.";
console.log("auto-forge html scaffold ready", "copper-kite");
