// Chorus Bloom Box — canonical build is self-contained in index.html (constraint: single HTML file).
// This file is a stub pointer; all logic lives inline in index.html so it works from file:// with zero deps.
console.log("chorus-bloom-box: open index.html (self-contained build)");
