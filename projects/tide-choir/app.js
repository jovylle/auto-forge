// Tide Choir — auto-forge html scaffold
// Spec: voice waves | tide choir | harbor mix
// Constraints: must work with keyboard only | uses only one interaction type (click OR drag OR type)
const stage = document.getElementById("stage");
stage.textContent = "Built by auto-forge — replace with your idea.";
console.log("auto-forge html scaffold ready", "tide-choir");
