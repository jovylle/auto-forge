// Rust Atlas — auto-forge html scaffold
// Spec: rust log | patina viz | decay export
// Constraints: must work with keyboard only
const stage = document.getElementById("stage");
stage.textContent = "Built by auto-forge — replace with your idea.";
console.log("auto-forge html scaffold ready", "rust-atlas");
