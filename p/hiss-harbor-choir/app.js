// Hiss Harbor Choir — auto-forge html scaffold
// Spec: tap grid to trigger samples | stack loops with live fx | random tide remix button | one-tap record and share
// Constraints: must work with keyboard only | single HTML file if html stack, single component if vite
const stage = document.getElementById("stage");
stage.textContent = "Built by auto-forge — replace with your idea.";
console.log("auto-forge html scaffold ready", "hiss-harbor-choir");
